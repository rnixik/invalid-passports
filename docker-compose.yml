version: "3.2"
services:
  app:
    build:
      context: ./app
    volumes:
    - ./app:/var/www/app
    - ./app/php.ini:/usr/local/etc/php/conf.d/custom.ini
    - sock:/sock
    working_dir: /var/www/app
    tmpfs: /mnt/tmpfs
  nginx:
    build:
      context: ./nginx
    volumes:
    - ./app/public:/var/www/app/public
    - sock:/sock
  redis:
    image: "redis"
volumes:
  sock:
